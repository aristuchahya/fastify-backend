# fastify-backend

This project is a backend service that uses framework **Fastify** .

## 🚀 Installation

Follow the steps below to install and run the project:
```bash
git clone
cd fastify-backend
npm i
npx prisma migrate dev
npm run dev
```
